# Copy to scripts/.env and fill in. scripts/.env is git-ignored.
# Used by test-glpi.sh (and future test scripts).

GLPI_URL=https://glpi.example.org
GLPI_USER_TOKEN=replace_me

# Optional — only needed if your GLPI instance requires an App-Token header.
# GLPI_APP_TOKEN=
